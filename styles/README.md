# styles/

`app/globals.css` (Tailwind layers + design tokens) is the primary
stylesheet and covers the whole app today.

This folder is reserved for any standalone CSS that doesn't belong in the
Tailwind layer system — e.g. print stylesheets, or vendor CSS overrides —
should the need arise. Nothing lives here yet.
